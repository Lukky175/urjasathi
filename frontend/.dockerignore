# Keep the Docker build context small and reproducible.
# Without this, `docker build` uploads node_modules/ and a stale dist/ into the
# build context — slow locally, and a stale dist/ can shadow the fresh one.
node_modules
dist
.env
.env.local
*.log
.DS_Store
.vscode
Dockerfile
.dockerignore
